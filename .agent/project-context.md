# 项目上下文

> 这是长期项目事实。由开发者维护。Agent 不得自行修改，除非开发者明确要求更新项目上下文。

## 1. 项目基本信息

- 项目名称：`<project-name>`
- 项目目标：`<what-the-project-does>`
- 最终用户：`<target-users>`
- 当前阶段：`<prototype/development/production>`
- 部署形态：`<local/web/cloud/desktop/mobile/...>`

## 2. 技术栈

- 前端：`<framework-and-version>`
- 后端：`<framework-and-version>`
- 数据库：`<database>`
- 包管理器：`<package-manager>`
- 运行环境：`<runtime-and-version>`
- 部署环境：`<deployment-environment>`

## 3. 主要目录与职责

| 路径 | 职责 |
|---|---|
| `<path>` | `<responsibility>` |

## 4. 常用命令

| 目的 | 命令 |
|---|---|
| 安装依赖 | `<command>` |
| 本地启动 | `<command>` |
| 单元测试 | `<command>` |
| 集成测试 | `<command>` |
| Lint | `<command>` |
| 类型检查 | `<command>` |
| 构建 | `<command>` |
| 格式化 | `<command>` |

## 5. 架构与约定

- 核心架构：`<architecture-summary>`
- 依赖方向：`<dependency-rules>`
- API 约定：`<api-conventions>`
- 错误处理：`<error-contract>`
- 日志约定：`<logging-conventions>`
- 测试约定：`<testing-conventions>`
- 命名与格式：`<naming-and-formatting>`

## 6. 用户上下文与本地化

- 默认语言：`<default-locale>`
- 默认时区：`<default-timezone>`
- 用户语言来源：`<profile/request/app-config/...>`
- 用户时区来源：`<profile/request/app-config/...>`
- 翻译资源位置：`<path-or-not-yet-created>`
- 时间存储策略：`<usually-UTC>`
- 时间展示策略：`<user-timezone-and-locale>`

## 7. 内容与 Prompt 管理

- 用户文案位置：`<path-or-service>`
- 模板位置：`<path-or-service>`
- 产品 Prompt 位置：`<path-or-service>`
- 运行时可编辑内容来源：`<database/CMS/config/not-applicable>`

## 8. 保护范围

- 未经明确要求不得修改：`README*`、`CHANGELOG*`、`LICENSE*`、`docs/`、`<other-paths>`
- 不得破坏的行为：`<invariants>`
- 禁止引入的依赖或技术：`<constraints>`
- 安全与隐私要求：`<requirements>`

## 9. 已知问题与风险

- `<known-issue-or-risk>`

